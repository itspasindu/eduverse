# Netlify — set site base directory to `frontend` in the Netlify UI (or deploy from this folder).

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
